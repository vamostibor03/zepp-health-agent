requests>=2.31.0,<3
python-dotenv>=1.0.0,<2
# Pin to the stable openai 1.x line. 2.x/3.x switched to the httpx2 stack
# which failed at the transport layer on CI runners.
openai>=1.40.0,<2
# MCP server (exposes the health history to Cursor/Claude via stdio and to
# ChatGPT via streamable-http). Only needed when running watchdata.mcp_server.
# Pin to the 1.x line: 2.x renamed FastMCP -> MCPServer and changed the API,
# which breaks watchdata/mcp_server.py.
mcp>=1.9.0,<2
